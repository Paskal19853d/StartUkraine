# Зоряна Пам'ять — ReSS configuration
# УВАГА: не публікуйте цей файл!

# JWT Secret (ОБОВ'ЯЗКОВО змініть на виробничому сервері!)
JWT_SECRET=ZoryanaP4myat_SecureKey_2024_!Change_In_Production

# Час дії токена (години)
TOKEN_EXPIRE_HOURS=8

# DB_PATH не вказано — використовується ../memorial.db відносно ReSS/
# Якщо потрібен інший шлях — розкоментуйте:
# DB_PATH=/absolute/path/to/memorial.db

# Дозволені origins (через кому)
ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# Адмін (використовується ТІЛЬКИ при першому запуску якщо немає адміна в БД)
ADMIN_EMAIL=admin@admin.com
ADMIN_PASS=Admin
