# SESSION_CHANGES.md — Журнал останньої робочої сесії (деплой-інструкція)

> **Навігація по документації проєкту:** [CLAUDE.md](CLAUDE.md) (читати ПЕРШИМ, головний довідник) · [SKILL.md](SKILL.md) (постійні навички: безпека + адаптив) · [DATABASE.md](DATABASE.md) (схема БД) · [MASTER_GUIDE.md](MASTER_GUIDE.md) (гайд розгортання) · [SECURITY_RULES.md](SECURITY_RULES.md) (політики безпеки) · [PRODUCTION.md](PRODUCTION.md) (чекліст продакшн)
>
> Цей файл — **доповнення** до `CLAUDE.md`, не заміна. Повний і актуальний опис усіх фіч, таблиць, ендпоінтів — завжди в `CLAUDE.md` (секції 4, 5, 10, журнал змін секція 15, версії v3.11–v3.18 нижче). Цей файл існує окремо як **швидкий чекліст деплою**: що саме зроблено за сесію, які файли залити, який SQL виконати, і в якому порядку.

---

## 1. Що зроблено за сесію (хронологічно)

| # | Версія | Що зроблено | Деталі |
|---|---|---|---|
| 1 | v3.11 | Онбординг-тур: фікс регресій (картка не відкривалась + мобільні підказки без стрілок) | `let`/`window` scoping баг + `#topbar` horizontal-scroll баг |
| 2 | v3.12 | Нова адмін-категорія «Підказки сайту» — увімк/вимк туру + відео-інструкція | `tour_enabled`, `tour_video_url` |
| 3 | v3.13 | Фікс підвисань диму над картою (карта, не дим) | Пауза WebGL-симуляції під час драгу карти |
| 4 | v3.14 | Фікс: модальні вікна `.mov`/`.mo` перекривались шапкою/пошуком/чатом | `z-index: 900 → 9990` |
| 5 | v3.15 | Фікс: `#partners-layer` і `#kyiv-clock` перекривали форму додавання (регресія v3.14) | `z-index → 890` для обох |
| 6 | v3.16 | Promo-сторінка (`/promo/`): реальна статистика замість статичних чисел + оновлено копірайт | `TREETEX NPO` → `ТМ «ЗОРЯНА ПАМ'ЯТЬ»` |
| 7 | v3.17 | **Новий модуль «Відео-попап» (реклама)** — адмінка + БД + серверна безпека | Див. розділ 3 нижче |
| 8 | v3.18 | Підказки сайту: окремий вимикач вікна відео-туру (незалежно від URL) | `tour_video_enabled` |
| 9 | v3.19 | Адмінка: відкладене збереження замість миттєвого — 5 модулів | Пристрої, Реєстрація, Карта світу, Google OAuth, Чат-налаштування |
| 10 | v3.20 | Фікс: карта світу показувала "API KEY REQUIRED" (CARTO вимагає ключ) | Нове поле CARTO-ключа в адмінці + синхронізовано з формою додавання запису |
| 11 | v3.21 | **Універсалізація**: категорії людей (військовий/цивільний/поза війною) + режим додавання free/request | Див. розділ 8 нижче |

Повний технічний опис кожного пункту — в [CLAUDE.md](CLAUDE.md), секція **«15. ЖУРНАЛ ЗМІН»** (шукайте заголовок `### v3.XX` відповідної версії).

**⚠️ Дія користувача потрібна для v3.20**: отримати безкоштовний CARTO API-ключ на [carto.com/basemaps/apikey](https://carto.com/basemaps/apikey/) і вставити в адмінці (розділ "Карта світу") — без цього кроку темна карта світу продовжить показувати "API KEY REQUIRED" (це не виправляється самим кодом, ключ видає CARTO на email).

---

## 2. Файли для деплою (FTP)

Змінені файли за всю сесію (звірено з `git status`):

```
Paskal.py             — backend: нові ключі colors, нові ендпоінти, нова таблиця, валідація, v3.21 категорії/free-request
index.html            — публічна сторінка: тур, дим/карта, модалки, відео-попап, v3.20 CARTO-ключ, v3.21 форма категорій
admin.html            — адмінка: нові секції "Підказки", "Реклама", v3.21 "Додавання людей" + v3.19 відкладене збереження
js/chat-admin.js      — v3.19: chatSaveSettings() замість миттєвої chatSaveCfg()
card.html             — v3.21: універсальні формулювання за категорією (fillPage())
seo_utils.py          — v3.21: умовні SEO title/description/keywords за категорією
templates/memorial.html — v3.21: умовний лейбл дати смерті/місця
promo/index.html      — /promo/ лендинг: реальна статистика + копірайт
CLAUDE.md             — документація (не впливає на роботу сайту, але рекомендовано заливати для консистентності)
```

**Порядок дій на проді:**
1. Залити всі файли вище через FTP.
2. Виконати SQL-міграції в PhpMyAdmin (розділ 3 нижче) — **обов'язково `USE zoryana_pamyat;` на початку**, інакше `#1046 База даних не вибрана`. Для v3.21 **обов'язково спочатку** `migrations_universal_memory.sql` (весь backend спирається на нові колонки), потім `migrations_i18n_universal_memory.sql`.
3. Перевірити в адмінці: розділ «Підказки» (тогл туру працює, новий тогл відео-вікна є), розділ «Реклама» (з'явився в меню, форма зберігається), розділ «Додавання людей» (v3.21, перемикач free/request зберігається).

---

## 3. SQL-міграції — що і в якому порядку виконати

> Пов'язано з [DATABASE.md](DATABASE.md) — там повна схема всіх таблиць, тут лише **дельта** цієї сесії.

### 3.1. `migrations_ad_video.sql` — модуль «Реклама» (новий, ще не виконаний)
Створює таблицю `ad_video_views` + 7 ключів `colors` (`ad_video_enabled`, `ad_video_url`, `ad_video_title`, `ad_video_preview_url`, `ad_video_channel_url`, `ad_video_channel_btn`, `ad_video_freq_days`).

### 3.2. `migrations_tour_video.sql` — модуль «Підказки»: базові ключі (з v3.12, якщо ще не виконано)
`tour_enabled`, `tour_video_url` + i18n ключі для відео-модалки туру.

### 3.3. `migrations_tour_video_toggle.sql` — окремий вимикач відео-вікна (новий, з v3.18)
Додає `tour_video_enabled` (дефолт `1` — без регресії для вже налаштованих сайтів).

**Всі три файли ідемпотентні** (`ON DUPLICATE KEY UPDATE` / `CREATE TABLE IF NOT EXISTS`) — повторний запуск не зашкодить.

⚠️ **Відома проблема, вже виправлена в файлах**: перша версія `migrations_tour_video_toggle.sql` мала занадто довгий текст у колонці `label` → `#1406 Data too long for column 'label'`. Виправлено — тепер короткий текст (`'Показ відео-вікна туру (1/0)'`). Якщо копіюєте SQL вручну з чату, а не з файлу — беріть актуальну версію з `migrations_tour_video_toggle.sql` на диску, не зі старих повідомлень.

### 3.4. `migrations_universal_memory.sql` + `migrations_i18n_universal_memory.sql` — v3.21 «Універсалізація» (нові, ще не виконані)
**КРИТИЧНО**: виконати `migrations_universal_memory.sql` **першим**, до заливки backend-файлів на прод (або одразу після) — `Paskal.py` з v3.21 звертається до нових колонок `memorials` (`category`, `death_reason`, `war_related`, `citizenship`, `nationality`, `created_by_uid`, `show_creator`) і нових ключів `colors` (`add_person_mode`, `callcenter_phone`, `callcenter_msg_uk`, `callcenter_msg_en`) — без міграції `POST/PUT` записів впаде з SQL-помилкою "unknown column". Другий файл (`migrations_i18n_universal_memory.sql`, ~90 рядків uk+en) можна виконати одразу після — без нього нові select'и в формі/адмінці просто покажуть порожні/англійські підписи замість перекладу, це не критично для роботи, але бажано зробити разом.

---

## 4. Новий модуль «Відео-попап» (реклама) — де що шукати

Пов'язано з [CLAUDE.md](CLAUDE.md) секції 4 (`ad_video_views`), 5 (`GET /api/ad-video/status`, `POST /api/ad-video/seen`, `POST /api/admin/upload/ad-preview`), 10 (`ad_video_*` ключі colors).

- **Адмінка**: бічне меню → «Реклама» (іконка мегафон) → toggle + поля URL/назва/прев'ю-картинка/канал/частота.
- **Публічна сторінка**: модалка `#ad-video-modal` в `index.html`, логіка `_adVideoCheck()`/`_showAdVideo()`.
- **Безпека**: анонімна cookie `zp_vid` (httponly, 400 днів), сервер сам звіряє `ad_video_enabled`+`ad_video_freq_days` — не покладається на клієнтський кеш. Rate-limit на обох нових ендпоінтах.
- **Звук**: автовідтворення зі звуком без кліка користувача браузери блокують завжди (не залежить від нас) — тому прев'ю-картинка з play-кнопкою, клік запускає iframe зі звуком.

---

## 5. Модуль «Підказки сайту» — фінальна логіка (3 стани)

Пов'язано з [CLAUDE.md](CLAUDE.md) секція 10 (`tour_*` ключі).

| `tour_enabled` | `tour_video_enabled` | `tour_video_url` | Результат |
|---|---|---|---|
| `1` | — | — | Покроковий тур (як завжди) |
| `0` | `1` | заповнений | Показується відео-вікно |
| `0` | `0` | заповнений | **Нічого не показується**, URL лишається збереженим на майбутнє |
| `0` | будь-що | порожній | Нічого не показується |

Адмінка: розділ «Підказки» → тогл «Тур підказок» → якщо вимкнено, з'являється тогл «Показувати відео-вікно» + поле URL.

---

## 8. Універсалізація (v3.21) — де що шукати

Пов'язано з [CLAUDE.md](CLAUDE.md) секція 15, `### v3.21` (повний опис).

- **Нові поля `memorials`**: `category` (`military`/`civilian_war`/`civilian`, default `military`), `death_reason` (код, набір залежить від category), `war_related` (0/1, похідне від category), `citizenship`/`nationality` (коди), `created_by_uid` (визначається сервером з cookie, НЕ з тіла запиту), `show_creator`.
- **Режим free/request**: адмінка → «Додавання людей» → перемикач + телефон + повідомлення uk/en. Серверна перевірка — `POST /api/people` (Paskal.py), 403 з `{code, message_uk, message_en, phone}` якщо `request`-режим і користувач не admin/moder.
- **Публічна форма** (`index.html`, `#madd`): нові select'и категорія/причина/громадянство/національність + toggle «показувати ім'я». При `request`-режимі форма замінюється повідомленням з кнопкою `tel:`.
- **card.html**: формулювання (лейбли/фолбеки/timeline) тепер залежать від `category` — стара військова картка виглядає ідентично, нові категорії показують нейтральні/цивільні формулювання, військовий блок повністю прихований (не «Звання: —»).
- **SEO**: title/description/keywords/JSON-LD залежать від category — див. `seo_utils.py` + `_build_memorial_seo()` в Paskal.py.
- **Довідники** — прості коди в `<select>`, без нових таблиць БД (свідоме рішення користувача).

---

## 6. Відомі відкриті питання / не перевірено

- **Диск C: користувача критично заповнений** (задокументовано в `CLAUDE.md`, згадується у всіх версіях v3.11–v3.18) — жодна зміна цієї сесії не перевірена скріншотами/функціонально в headless-браузері. Усі фікси базуються на прямому читанні коду й підтвердженій специфікації (JS `let`/`window`, CSS `overflow-x`), не на здогадках — але візуальна перевірка на проді після деплою рекомендована для кожного пункту.
- **Модуль «Реклама» не увімкнений за замовчуванням** (`ad_video_enabled=0`) — нульовий вплив на відвідувачів, поки адмін явно не заповнить поля і не увімкне тогл.
- **v3.21 (Універсалізація) не перевірена функціонально** — залежить від виконання SQL-міграцій на проді (розділ 3.4), без яких `POST/PUT` записів у Paskal.py впаде з SQL-помилкою. Дефолти (`category='military'`, `add_person_mode='free'`) не змінюють поведінку для вже існуючих сайтів одразу після міграції — старі військові записи і форма додавання виглядають як раніше, доки адмін свідомо не увімкне request-режим або не додасть цивільний запис.

---

## 7. Як користуватись цим файлом

Це **живий чекліст**, не архів — коли наступна сесія вносить нові зміни, сюди додається новий рядок у таблицю розділу 1 (не переписувати історію), і за потреби — новий розділ із деталями. Повна історія версій (без обмежень актуальності) завжди в `CLAUDE.md` секція 15.
